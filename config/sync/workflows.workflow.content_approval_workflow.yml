uuid: 0deab357-db85-4fb1-8d37-fc3af06a5e43
langcode: en
status: true
dependencies:
  config:
    - entityqueue.entity_queue.hall_of_fame_order
    - entityqueue.entity_queue.leadership_team_order
    - entityqueue.entity_queue.methodology_and_quality_measures
    - node.type.asb_briefing
    - node.type.asb_notice
    - node.type.landing_page
    - node.type.news_release
    - node.type.page
    - node.type.staff
  module:
    - content_moderation
    - paragraphs_library
id: content_approval_workflow
label: 'Content Approval Workflow'
type: content_moderation
type_settings:
  states:
    draft:
      label: Draft
      weight: 0
      published: false
      default_revision: false
    needs_editing:
      label: 'Needs Editing'
      weight: 3
      published: false
      default_revision: false
    published:
      label: Published
      weight: 1
      published: true
      default_revision: true
    ready_for_review:
      label: 'Ready for Review'
      weight: 2
      published: false
      default_revision: false
  transitions:
    create_new_draft:
      label: 'Create New Draft'
      from:
        - draft
        - published
      to: draft
      weight: 0
    publish:
      label: Publish
      from:
        - draft
        - published
      to: published
      weight: 1
  entity_types:
    entity_subqueue:
      - hall_of_fame_order
      - leadership_team_order
      - methodology_and_quality_measures
    node:
      - asb_briefing
      - asb_notice
      - landing_page
      - news_release
      - page
      - staff
    paragraphs_library_item:
      - paragraphs_library_item
  default_moderation_state: draft
