[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nass-feeds-csv-generator"
version = "0.1.0"
description = "Generate Feeds-compatible CSVs for NASS Drupal migration."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
